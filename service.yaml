apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
spec:
  type: LoadBalancer
  selector:
    app: simple-web-app
    color: blue   # The router starts by pointing to the Blue environment
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 3000