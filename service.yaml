apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
spec:
  type: LoadBalancer
  selector:
    app: simple-web-app
  ports:
    - protocol: TCP
      port: 80         # The port exposed to the internet
      targetPort: 80 # The port your Flask app is running on inside the container